// Admins table is intentionally not used in Sprint 1 — admin auth
// is handled via env vars (ADMIN_EMAIL / ADMIN_PASSWORD). Keep this
// stub so future sprints can introduce a real admins table without
// reorganising the schema package.
export {};
